# 更新日志

本项目所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [v2.0] - 2026-08-16

### ✨ 新增

- **一键零配置启动**：Node.js → DSH → 插件三级自动引导。全部就绪时直接进入主界面、不弹安装窗；DSH 缺失时自动全局安装 `@deepseek-ai/dsh`。
- **Node.js 缺失双选项安装窗**：`winget` 一键自动安装 / 手动下载官方 MSI 后点「我已安装」自动检测，可随时在两方式间切换；Node.js 安装为硬阻塞项、不可跳过。
- **自动安装两个精选插件**：`dsh-remote-gateway`（手机远程访问，Cloudflare Quick Tunnel + 独立登录认证）与 `dsh-market`（DSH 内置插件市场，800+ 插件浏览 / 一键安装 / 主题 / 备份），均在 DSH 设置内可见。
- **服务守护线程（v2 招牌特性）**：`dsh web` 意外退出（如插件一键重启失败）时，8 秒宽限期后自动重新拉起并刷新窗口；插件自行重启成功时自动让出管理权、不误杀新实例；复用外部实例时不干预。
- **隐藏控制台启动**：`dsh web` 以隐藏控制台运行，agent 执行终端命令不再弹出黑窗，日志仍写入文件。
- **启动流程重构**：全部就绪时直接开主界面（无安装窗闪烁），安装窗按需出现。
- 自动安装依赖辅助：自动装 pnpm、自动处理 allowBuilds 构建许可（含精确键解析与占位值覆盖）、git 缺失提示。

### 🔧 修复

- 修复 `dsh-market` 1.4.1 在 Windows 上「一键重启」后服务无法恢复的问题（守护线程兜底，端到端验证：杀服务 → 自动拉起 → 窗口刷新）。
- 修复插件安装 allowBuilds 键不匹配（简单键 vs pnpm 精确键）导致全新机器安装失败的问题。
- 修复 pnpm 自动写入的「set this to true or false」占位值未被覆盖导致重装失败的问题。
- 修复 winget 安装 Node.js 后 PATH 未刷新导致误报失败的问题（常见安装目录兜底探测 + PATH 注入）。
- 修复错误信息可能被当作 HTML 注入的问题（改用 `textContent` 渲染）。
- 修复安装窗一启动就闪现「正在安装」的误导提示。

### ⚡ 优化

- 插件安装失败可「重试 / 仍然继续」，Node 安装硬阻塞、不可跳过。
- venv 干净构建（仅必要依赖），exe 保持约 15MB。
